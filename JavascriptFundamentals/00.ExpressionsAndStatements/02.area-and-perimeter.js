function solve([a, b]) {
    let width = Number(a);
    let height = Number(b);

    let area = width * height;
    let perimeter = (2 * width) + (2 * height);

    console.log(area);
    console.log(perimeter);
}