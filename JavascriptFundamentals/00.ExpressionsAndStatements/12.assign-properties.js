function solve([firstProp, firstVal, secondProp, secondVal, thirdProp, thirdVal]) {
    let obj = {
        [firstProp]: firstVal,
        [secondProp]: secondVal,
        [thirdProp]: thirdVal
    };

    return obj;
}